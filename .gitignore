# Claude Code local settings
.claude/settings.local.json

# Skill installations (npx skills add) — these are agent tooling, not app code
.agents/
.claude/skills/
skills-lock.json

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Environment variables — never commit real keys
.env.local
.env

# Generated by build.js on each deploy — never commit
dist/
config.js

# Cleanup artifacts
write_index.js
index.html.bak

# Node dependencies and one-time setup scripts
node_modules/
package-lock.json
setup.js
