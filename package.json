{
  "name": "work-effort-tracker",
  "version": "2.0.0",
  "description": "Personal work effort tracker with Supabase backend",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve . -p 3000"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "node-appwrite": "^25.2.0"
  }
}
