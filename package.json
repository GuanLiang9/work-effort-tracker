{
  "name": "work-effort-tracker",
  "version": "2.0.0",
  "description": "Personal work effort tracker with Appwrite backend",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist -p 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
